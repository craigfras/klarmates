/**
 * Shared user-facing UI strings.
 *
 * Lightweight feedback/messaging constants used across feature views so the
 * same copy is never duplicated (DRY). Pure data — no logic, no JSX.
 */

// ---------------------------------------------------------------------------
// Error messaging
// ---------------------------------------------------------------------------

/** Generic fallback shown when an action fails without a server message. */
export const DEFAULT_ERROR_MESSAGE = "Something went wrong. Please try again.";
